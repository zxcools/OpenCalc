# 期货开仓计算器 — 交付说明 (v12)

## 交付物
| 文件 | 说明 |
|---|---|
| `dist/OpenCalc.exe` | 单文件程序（约 10MB），双击即用，无需安装 Python |
| `桌面「期货开仓计算器」快捷方式` | 已创建并验证指向最新版 |

## 使用方式
双击桌面 **「期货开仓计算器」** 图标 → 弹出**独立应用窗口**（Chrome 无地址栏模式，体验接近原生软件）。页面右上角 **✕** 退出；空闲 2 小时后自动退出。

## 两大功能模块（顶部 tab 切换）

### 🧮 模块一：开仓计算（已完成）
- 期货模式 / 期权模式 切换
- **期货**: 开仓金额 = 权益 × 1.5%, 最大手数 = 开仓金额 ÷ 每手风险金额(止损价差×乘数)
- **期权**: IV<50% → 权益 × 2%; IV≥50% → 权益 × 1.5%
- 盈亏比阈值 1.5; 颜色：风险绿、盈利红; 手数不足 1 手强制不开仓
- 主力合约实时行情（自动探测持仓量最大合约, 显示具体月份如 RB2610）; 10 秒自动刷新
- 75+ 品种全支持（含硅铁 SF、锰硅 SM 等活跃品种）
- 窗口/任务栏图标：与桌面图标一致（「仓」字金色图标）

### 📈 模块二：资金曲线（v12 新增）
- **顶部策略切换**：abe（默认） / 威科夫 / 汇总
- **月度明细**：8 列 — 年月 / 月初权益 / 本月末权益（扣掉出入金之后）/ 出入金（出金为正）/ 本月盈亏 / 本月收益率 / 备注 / 操作
  - **本月盈亏** = 本月末权益 − 月初权益 + 出入金（出金为正）
  - **本月收益率** = 本月盈亏 ÷ 月初权益
- **年度汇总**（自动从月度汇总累加）：
  - 年初权益 = 该年首月月初权益
  - 年末权益 = 该年最后月月末权益
  - 总出入金 = ∑ 月度出入金
  - 年度总盈亏 = 年末权益 − 年初权益 + 总出入金
  - 年化收益率 = 年度总盈亏 ÷ 年初权益
- **仪表盘**（按策略自动生成 5 张图）：
  1. abe 月收益图（柱：本月末权益/本月盈亏 + 折线：本月收益率 %）
  2. abe 年盈亏分析（柱：年末权益/年度总盈亏 + 折线：年化收益率 %）
  3. 威科夫 月收益图（同上）
  4. 威科夫 年盈亏分析（同上）
  5. 汇总 月/年图（两个策略同期相加）
- **数据存储**：本地 `data/funds.db`（SQLite），数据不出本机
- 操作：「＋ 记录月度」按钮 → 表单填写 → 自动重算汇总与图表；月明细行可单条删除

## 验证
- 67 项测试全部通过（含 27 项新资金曲线测试：公式、upsert/删除、跨年汇总、跨账户汇总、错误拒绝）
- v12 exe 端到端验证：API、CRUD、图表数据完整可用
- Chart.js 本地打包（200KB），离线完全可用

## 注意事项
- 行情来自新浪财经公开接口，盘中实时、有约 20 秒延迟；测算为本地计算无需联网
- 基金曲线数据仅保存在本机 SQLite，不会泄露
- 源码：`main.py`（重新打包前先把 `dist/OpenCalc.exe` 移走避免覆盖冲突）
- 打包命令：`pyinstaller --onefile --windowed --name OpenCalc --icon icon.ico --add-data "icon.ico;." --add-data "chart.min.js;." main.py`
