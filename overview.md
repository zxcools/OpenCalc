# 开仓计算器 v50.29 — 合约代码强制规范写法

## 问题
筛选合约下拉里出现 `br2610C15800` 和 `br2610c15800` 两行 —— 同一个合约，只因一个大写 C、一个小写 c，被软件当成两个合约。

## 解决
新增 `normalize_contract()`，前后端各一份、规则一致：

```
品种代码 → 小写
看涨/看跌的 C/P → 大写（只认「月份数字后面」的那一位）
```

`BR2610c15800` → `br2610C15800`

### 关键点
- **只改 C/P，不动品种代码**：玉米 `c`、棉花 `cf`、纸浆 `sp`、棕榈油 `p`、聚丙烯 `pp`、苹果 `ap`、花生 `pk` 这些品种代码本身就带 C/P，整串盲目大写会把代码改坏。所以用正则只命中「月份数字后面」的 C/P。
- **接入点全覆盖**：前端录入 + 后端保存 + 导入备份 + 监控池快照，四条路径都过同一套规则。
- **历史记录自动归并**：保存时把「同一标的、同一合约（忽略大小写）、且除 id/时间戳外所有字段完全相同」的旧记录清掉，只留最新一条，旧的大小写混写记录自动消失。
- **去重必须逐字段全量比较**：只比 qty 和日期会误删价格不同的真实记录（「开 5 手 @500」和「开 5 手 @200」会被当成重复）。这一条是开发中实际踩到的坑，靠 E2E 持仓均价断言暴露出来。

## 涉及文件
| 文件 | 改动 |
|---|---|
| `main.py` | `normalize_contract()`；`trade_upsert` / `trade_import_record` / `trade_pool_upsert` / `trade_pool_import_record` 接入；前端 `TradeUI.normalizeContract()` + 输入框 blur 回填 + 筛选下拉按归一化去重 |
| `tests.py` | 163 → 170 项（归一化 16 组用例、非字符串透传、合并/并存、平仓余量匹配、导入归一化） |
| `oc_cdp_test.js` | 72 → 76 项（新增场景 P 4 项）；固定 1600×1000 视口，消除随窗口宽度漂移的假失败 |
| `README.md` | 新增 v50.29 版本说明；测试数同步更新 |

## 验证
- 后端 `python tests.py` — 170/170 通过
- E2E `node oc_cdp_test.js` — 76/76 通过
- exe 冒烟 — 大小写混写的两条开仓落库后只剩 1 个合约 `br2610C15800`，持仓 6 手 @703.33

## 交付
- `dist/OpenCalc.exe`（v50.29，双击即用）
- commit `e3b5d4e`

---

# 历史交付说明 (v12)

## 交付物
| 文件 | 说明 |
|---|---|
| `dist/OpenCalc.exe` | 单文件程序（约 10MB），双击即用，无需安装 Python |
| `桌面「期货开仓计算器」快捷方式` | 已创建并验证指向最新版 |

## 使用方式
双击桌面 **「期货开仓计算器」** 图标 → 弹出**独立应用窗口**（Chrome 无地址栏模式，体验接近原生软件）。页面右上角 **✕** 退出；空闲 2 小时后自动退出。
