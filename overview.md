# 开仓计算器 v50.31 — 数据安全专项（更新软件不再丢数据）

## 你的问题
> 交易记录保存在 `D:\Workbuddy\开仓计算器\data\funds.db`，更新软件时这个文件会重置，之前的记录全没了。

## 排查结果：两个原因，第二个才是主因

### 原因 1：数据被设置在了「软件自己的文件夹」里
exe 读取的配置是 `C:\Users\Administrator\AppData\Roaming\OpenCalc\config.json`，里面存着：
```json
"data_dir": "D:\\Workbuddy\\开仓计算器\\data"
```
于是数据躺进了软件目录内部。**更新软件 = 替换/清理这个目录 → 数据跟着被删。**

### 原因 2（主因）：测试脚本会清空真实数据库
`tests.py` 里硬编码了真实库路径，并且直接删掉三张表：
```python
_conn3 = sqlite3.connect(os.path.join(os.path.dirname(__file__), "data", "funds.db"))
for t in ("records", "trade_records", "trade_pool_snapshots"):
    _conn3.execute(f"DELETE FROM {t}")     # ← 共 6 处
```
而我每轮「更新软件」都要跑一次测试 → **跑一次就把你的记录清空一次**。这正好解释了「一更新就重置」。

## 已修复

### 1. 数据搬到软件目录之外
数据现在在 **`D:\Workbuddy\OpenCalc数据\funds.db`** —— 软件目录之外，又在你云盘同步的 `D:\Workbuddy` 里。
exe 的配置和开发用的配置都已指向它。**以后更新软件只替换程序文件，碰不到这份数据。**

### 2. 启动就检查数据位置，落在软件目录内会明确报警
- 侧栏 ⚙ 按钮亮**红点**
- 页面顶部弹**红色横幅**（非阻塞、不挡操作），带「一键迁出」按钮
- 「⚙ 数据位置」弹窗里也有醒目警告 + 一键迁出

### 3. 每次数据变动自动备份
新增 / 修改 / 删除交易记录、资金曲线、监控池 → 立刻往 `<数据目录>\backup\` 存一份带时间戳的快照，**自动保留最近 20 份**。
- 用 SQLite 官方备份接口，不会拿到写了一半的库
- 「导入备份」和「清除全部」会在操作**之前**先备份 —— 误删可以退回
- 每次启动也备份一次；弹窗可看备份位置 / 份数 / 最近时间，并有「立即备份」按钮

### 4. 换数据目录不再丢交易记录
之前切换数据位置**只迁移资金曲线**，交易记录和监控池会静默消失。现在三张表全部迁移，反复切换也不会翻倍。

### 5. 测试与真实数据彻底隔离（防止问题复发）
- `tests.py`：全程使用临时数据库，真实库只读不写；路径不对会**拒绝运行**
- `oc_cdp_test.js`：先确认服务用测试目录启动，否则**拒绝运行**（实测对着真实数据实例会 exit 2）
- 新增 `OC_NO_TAKEOVER=1` + `OC_PORT`：不影响你正在使用窗口的前提下另开实例做测试

## 你的数据现状（已核实）
从事故前的安全备份完整恢复，哈希校验一致：

| 表 | 条数 | 内容 |
|---|---|---|
| 资金曲线 | 10 | 2025-11 ~ 2026-08 |
| 交易记录 | 24 | br2610多 / cf2701多 / fu2611多 / jd2610空 / lg2611多 / px2611多 / si2611空 / ta2611多 |
| 监控池 | 1 | 12 个品种快照 |

- **数据位置**：`D:\Workbuddy\OpenCalc数据\funds.db`
- **安全副本**：`C:\Users\Administrator\AppData\Roaming\OpenCalc\safety_backup\funds_20260910_212919.db`

## 需要你做的一件事
当前开着的是**旧程序**（还指着旧的空目录）。请**关掉它，再从桌面快捷方式重新打开**，新版本会读到 `D:\Workbuddy\OpenCalc数据\` 里的完整数据。

> 旧目录 `D:\Workbuddy\开仓计算器\data\` 里那份已经没用了（空库），留着不管或自己删掉都行。

## 涉及文件
| 文件 | 改动 |
|---|---|
| `main.py` | `_app_dir()` / `_is_inside()` / `data_dir_risky()` / `suggest_safe_data_dir()`；`OC_DATA_DIR` 优先级；`fund_auto_backup()` / `fund_backup_list()`；7 个写接口挂自动备份；`fund_set_data_dir` 迁移三表；`OC_NO_TAKEOVER`；顶部警告横幅 + 弹窗警告 + 一键迁出 + 立即备份 |
| `tests.py` | 开头即隔离数据库与配置 + 拒绝运行守护；6 处硬编码真实库路径替换；新增 21 项数据安全断言 → **209 项** |
| `oc_cdp_test.js` | `guardRealData()` 数据闸门 + `OC_E2E_BASE` 可指定端口；新增场景 Q（横幅/红点/迁出按钮/弹窗备份信息/立即备份/备份列表）→ **95 项**；顺手修掉两条时间/转义相关的脆弱断言 |
| `README.md` | 新增 v50.31 版本说明；测试数同步更新 |

## 验证
- 后端 `python tests.py` — **209/209 通过**，跑完真实库哈希不变（零触碰）
- E2E — **95/95 通过**；数据闸门实测：对着真实数据实例**拒绝运行**（exit 2）
- 前端 JS 语法校验通过
- exe 冒烟 — version 51，指向新数据目录，自动备份生效

## 交付
- `dist/OpenCalc.exe`（v50.31，双击即用）
- `backup/OpenCalc_v50.31.exe`

---

# 开仓计算器 v50.30 — 分隔式合约识别 + 全功能巡检

## 需求一：分隔式期权合约自动识别
`lc2611-C-144000` 这种 C/P 前后用 `-` 隔开的写法，输入时无法自动识别看涨看跌，需加规则。

### 解决
重写 `find_cp_index()`（前后端各一份，规则一致），支持三类写法：

| 写法 | 示例 | 识别 |
|---|---|---|
| 紧凑式 | `sf611c6800` / `ao611P2500` | ✅ |
| 分隔式 | `lc2611-C-144000` / `lc2611_C_144000` | ✅ |
| 半分隔式 | `lc2611-c144000` | ✅ |

### 关键点
- **取「最右侧」满足条件的位置**：`hc2610P6000` 里品种前缀 `hc` 的 `c` 和真实的 `P` 都「看着合法」，取最右才是标志位。
- **判定必须依赖月份**（3-4 位数字段）：否则 `hc2610` 里品种前缀的 `c` 会被当成标志位。
- **缺月份的写法不认**：`aoc5000` / `aoC5000` / `ao-c-5000` 无法与品种前缀区分，且真实期权代码必含月份，一律不识别。
- **分隔符规范**：空白直接删（随手敲的）、`_` `/` 统一成 `-`；非 C/P 两侧的 `-` 去掉。`br 2610 C 15800` → `br2610C15800`。

## 需求二：完整功能测试 — 发现 2 个此前未暴露的 bug

### 🐛 Bug 1：负数手数能存进库（重要）
开仓手数填负数时记录能保存成功（提示「已保存」），但持仓汇总按 `qty > 0` 计算 → 该记录在持仓里查不到、界面上也看不到，成了隐形脏数据。
**修复**：开仓手数必须为正整数；开仓价 / 权利金不得为负。

### 🐛 Bug 2：风险额度任意值可存进配置
`futures_risk_pct` / `options_risk_pct` 此前任意数字都能写进配置文件（如 999）。前端虽按 `<option>` 匹配后不会应用，但配置里留脏值易误导后续维护。
**修复**：只接受 0.5 / 1 / 1.5 / 2 / 3 五档，其余拒绝。

### 巡检覆盖范围（68 项，全端点 + 边界）
基础端点存活、合约表完整性、期货/期权计算（正常值 + 权益为0/为负/止损=入场/方向反了/缺字段/超大值）、设置读写往返、交易记录 CRUD（超额平仓/负数量/非法日期/非法 op_type/缺字段）、监控池 CRUD、资金曲线 CRUD、行情接口、静态资源。

> 其余「失败项」经核对均为巡检脚本自身的 API 载荷写法错误（如 `code` 应填品种代码 `cu` 而非 `cu2610`、字段名为 `risk_ratio` 而非 `risk_pct`），非软件 bug。

## 涉及文件
| 文件 | 改动 |
|---|---|
| `main.py` | `find_cp_index` / `_tidy_seps` / `_collapse_seps` / `_has_month` 重写；`detect_call_put` 新增；`trade_upsert` 加开仓量价校验；`save_settings` 加风险额度档位校验；前端 `TradeUI.tidySeps/findCpIndex/normalizeContract/detectCallPut` 同步 |
| `tests.py` | 170 → 188 项（开仓参数校验 8 项、风险额度校验 4 项、归一化扩至 28 组、看涨看跌 21 组、216 例自带 C/P 品种） |
| `oc_cdp_test.js` | 76 → 82 项（场景 P2 6 项：分隔式归一化 / 半分隔 / `_` 统一 / 前缀取最右 / 识别 C/P / 缺月份不认） |
| `README.md` | 新增 v50.30 版本说明；测试数同步更新 |

## 验证
- 后端 `python tests.py` — **188/188 通过**
- E2E `node oc_cdp_test.js` — **82/82 通过**
- 前后端一致性 264/264（抠出前端方法跑同批用例）
- 前端 JS 语法校验 — JS OK
- exe 冒烟 — version 50，负手数被拒、999 被拒、期货计算正常、干净退出

## 交付
- `dist/OpenCalc.exe`（v50.30，双击即用）
- `backup/OpenCalc_v50.30.exe`

---

# 开仓计算器 v50.29 — 合约代码强制规范写法

## 问题
筛选合约下拉里出现 `br2610C15800` 和 `br2610c15800` 两行 —— 同一个合约，只因一个大写 C、一个小写 c，被软件当成两个合约。

## 解决
新增 `normalize_contract()`，前后端各一份、规则一致：

```
品种代码 → 小写
看涨/看跌的 C/P → 大写（只认「月份数字后面」的那一位）
```

`BR2610c15800` → `br2610C15800`

### 关键点
- **只改 C/P，不动品种代码**：玉米 `c`、棉花 `cf`、纸浆 `sp`、棕榈油 `p`、聚丙烯 `pp`、苹果 `ap`、花生 `pk` 这些品种代码本身就带 C/P，整串盲目大写会把代码改坏。所以用正则只命中「月份数字后面」的 C/P。
- **接入点全覆盖**：前端录入 + 后端保存 + 导入备份 + 监控池快照，四条路径都过同一套规则。
- **历史记录自动归并**：保存时把「同一标的、同一合约（忽略大小写）、且除 id/时间戳外所有字段完全相同」的旧记录清掉，只留最新一条，旧的大小写混写记录自动消失。
- **去重必须逐字段全量比较**：只比 qty 和日期会误删价格不同的真实记录（「开 5 手 @500」和「开 5 手 @200」会被当成重复）。这一条是开发中实际踩到的坑，靠 E2E 持仓均价断言暴露出来。

## 涉及文件
| 文件 | 改动 |
|---|---|
| `main.py` | `normalize_contract()`；`trade_upsert` / `trade_import_record` / `trade_pool_upsert` / `trade_pool_import_record` 接入；前端 `TradeUI.normalizeContract()` + 输入框 blur 回填 + 筛选下拉按归一化去重 |
| `tests.py` | 163 → 170 项（归一化 16 组用例、非字符串透传、合并/并存、平仓余量匹配、导入归一化） |
| `oc_cdp_test.js` | 72 → 76 项（新增场景 P 4 项）；固定 1600×1000 视口，消除随窗口宽度漂移的假失败 |
| `README.md` | 新增 v50.29 版本说明；测试数同步更新 |

## 验证
- 后端 `python tests.py` — 170/170 通过
- E2E `node oc_cdp_test.js` — 76/76 通过
- exe 冒烟 — 大小写混写的两条开仓落库后只剩 1 个合约 `br2610C15800`，持仓 6 手 @703.33

## 交付
- `dist/OpenCalc.exe`（v50.29，双击即用）
- commit `e3b5d4e`

---

# 历史交付说明 (v12)

## 交付物
| 文件 | 说明 |
|---|---|
| `dist/OpenCalc.exe` | 单文件程序（约 10MB），双击即用，无需安装 Python |
| `桌面「期货开仓计算器」快捷方式` | 已创建并验证指向最新版 |

## 使用方式
双击桌面 **「期货开仓计算器」** 图标 → 弹出**独立应用窗口**（Chrome 无地址栏模式，体验接近原生软件）。页面右上角 **✕** 退出；空闲 2 小时后自动退出。
